const Header = require("./header.component");
const Menu = require("./menu.component");
const Entry = require("./entry.component");
const Search = require("./search.component");

export default {
  Header: Header,
  Menu: Menu,
  Entry: Entry,
  Search: Search
};
